[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hack_ras"
version = "0.1.0"
description = "Python tools for HEC-RAS parsing and manipulation"
requires-python = ">=3.9"
